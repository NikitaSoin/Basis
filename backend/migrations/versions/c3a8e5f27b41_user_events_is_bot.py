"""user_events.is_bot: отделить роботов от людей

Владелец 2026-08-02 спросил, сколько уникальных людей заходило на платформу. Ответ по
сырым данным был бы неверным: за первые 12 часов работы лога записалось 382 «устройства»,
и почти все они — поисковый робот. Признаки однозначные: 370 из 382 сделали по 1–2
события, обошли 426 РАЗНЫХ страниц, шли ровным потоком по 50 устройств в час всю ночь и
все без источника перехода. Начался поток через час после отправки 7860 адресов в
IndexNow — то есть это Яндекс пришёл по нашему же приглашению.

Без разделения любая продуктовая метрика превращается в статистику обхода: «382
посетителя» при двух реальных зарегистрированных пользователях.

🔴 КОЛОНКА NULLABLE НАМЕРЕННО. NULL = «определить не могли»: строки, записанные до
появления детектора. Ставить им false значило бы объявить роботов людьми и получить
искажение навсегда; ставить true — записать в роботы возможных живых посетителей.
Честнее оставить неизвестность видимой: запросы по людям фильтруют `is_bot IS FALSE`.

Revision ID: c3a8e5f27b41
Revises: b2d7f4c19a83
"""
from alembic import op
import sqlalchemy as sa


revision = "c3a8e5f27b41"
down_revision = "b2d7f4c19a83"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.add_column("user_events", sa.Column("is_bot", sa.Boolean, nullable=True))
    # Основной сценарий — «покажи людей за период», поэтому индекс по паре.
    op.create_index("ix_user_events_bot_time", "user_events", ["is_bot", "created_at"])


def downgrade() -> None:
    op.drop_index("ix_user_events_bot_time", table_name="user_events")
    op.drop_column("user_events", "is_bot")
