# Дизайн-бэклог (отложенный фидбэк ИИ-персоны по /_design)

Зафиксировано 2026-06-02. Это НЕ для Фазы 2 (примитивы) — вернуться в Фазах 3-4
(перевод реальных страниц на дизайн-систему). Здесь только то, что трогает
существующие страницы/UI и сознательно отложено по правилу «сначала примитивы».

## Фаза 3 — перевод страниц на примитивы / структура
- **Табы на карточке компании** — переразвести на примитив `Tabs` (ARIA,
  accent-подчёркивание), убрать самописный разнобой.
- **Лендинг-CTA** — пересобрать на примитив `Button` (варианты/размеры),
  единый сигнатурный акцент.
- **Bento-иерархия** — навести визуальную иерархию блоков на страницах
  (карточка компании / обзор): размеры, плотность, группировка по важности.
- **Hover-строки и таблицы реальных данных** — после Фазы 2 перевести
  финансовые таблицы карточки на примитив `Table`.

## Фаза 4 — навигация, бренд, моушн
- **Иконки без подписей в сайдбаре** — добавить подписи/тултипы, проблема
  распознавания пунктов навигации.
- **Логомарк** — разработать знак бренда (сейчас его нет/слабый).
- **Count-up анимации** — пересмотреть; по конституции «не праздновать
  изменение цены». Убрать count-up при фоновом обновлении котировок, оставить
  максимум мягкое появление.

## УТВЕРЖДЁННЫЙ язык «Живость и глубина» (2026-06-03) — применять при переводе страниц
Витрина одобрена владельцем (секция «Живость и глубина» на /_design, `src/design/LiveDepthShowcase.jsx`).
Занесено в конституцию `basis-designer.md`. При переводе КАЖДОЙ оставшейся страницы — закладывать:
- ГЛУБИНА: карточки/KPI не плоские (тени shadow-sm/md/lg в светлой, слой+1px в тёмной), дозированно.
- ЦВЕТ В ДАННЫХ: графики с заливкой площади, градиентные спарклайны, полосы метрик осмысленными
  цветами (Okabe-Ito --cat-1..8), цветные бейджи секторов, treemap по динамике; success/danger —
  дельты; кобальт — кнопки/хром. 2-3 акцента на экран, не радуга.
- ДВИЖЕНИЕ (полноценно — Фаза 4; точечно — уже при переводе, где уместно): count-up при ПЕРВОЙ
  загрузке, прорисовка графиков, hover-подъём, slide таба, ≤1 медленный orbit/дышащий декор на раздел.
  150-250 мс, prefers-reduced-motion гейтит всё. Без новых пакетов.

## Чистка примитивов перед Фазой 3 (из ревью basis-design-reviewer, 2026-06-02)
Не блокеры — мелкие правки в `src/design/*`, закрыть до перевода страниц на примитивы:
- danger-`Button`: `tw-text-white` → токен (`--on-danger` или аналог), убрать литерал цвета.
- Удалить блок `ACCENT_CANDIDATES`/`AccentCandidates` из `DesignSystem.jsx` (кобальт выбран; в нём остался хардкод-hex).
- `Tooltip`: связать с триггером через `aria-describedby` (доступность).
- `Modal`: добавить focus-trap (сейчас Tab уходит за пределы).
- Спарклайн KpiTile: брать знак цвета из того же источника, что и `delta` (а не first-vs-last).
- `Card` в тёмной теме: убрать двойную 1px-обводку (border-strong + shadow-sm).
- Задокументировать нейтральный глиф дельты `▬` в конституции (чтобы fact-checker не флагнул).

## Мелочь на потом (из финального ревью полировки)
- `Modal` backdrop: использовать полупрозрачный scrim (затемнение под панелью),
  сейчас оверлей и панель визуально близки — уточнить при работе с модалками в Фазе 3.

## Решённое в Фазе 2 (для истории)
- Контраст muted-текста в тёмной теме (≥AA), читаемость тоггла темы.
- Единая утилита формата чисел (рус. locale) в Table/KpiTile.
- Трекинг на капс-микролейблах.
- Спарклайны: заливка площади + точка последнего значения.
- Консистентные hover-строки таблицы в обеих темах.
- Сигнатурный бренд-акцент (выбор владельца) → токен `--accent`.
