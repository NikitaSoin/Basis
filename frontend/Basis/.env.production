REACT_APP_API_URL=https://nikitasoin-basis-a772.twc1.net
GENERATE_SOURCEMAP=false

# Яндекс.Метрика — идентификатор счётчика (число из интерфейса Метрики).
# ПУСТО = аналитика ВЫКЛЮЧЕНА ПОЛНОСТЬЮ: скрипт счётчика не загружается, наружу не
# уходит ни одного запроса (проверено в собранном бандле, см. src/analytics.js).
# Чтобы включить — вписать номер и пересобрать фронт. Секретом не является: номер
# счётчика виден в исходном коде любой страницы с Метрикой.
REACT_APP_METRIKA_ID=
