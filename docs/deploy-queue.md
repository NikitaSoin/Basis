# Очередь push/деплоя — семафор, НЕ журнал

Этот файл живёт в состоянии «сейчас», не копится историей. Перед `git push`
прочитай его целиком. Правило и причина — в `CLAUDE.md`, раздел «Параллельные
сессии» (⚙️ Технические правила).

## Статус: ЗАНЯТО

- Сессия: SEO / потоки спроса — индексы и дивиденды
- Начала: 2026-08-03 13:50
- Пушит: СВОДНЫЙ ПУШ (правило владельца 2026-08-02) — 4 коммита:
  свои 3 (состав индексов + перелинковка, дивидендный лендинг с данными) и
  0487a8cdcf «премия к Brent» соседней сессии (готовая работа с тестами,
  лежала незапушенной)
- Ждёт: смены `main.<hash>.js` на бою + `verify-live.js`

<!-- Очередь сессии «институты» снята мной 2026-08-03 13:50: её работа (23148d1f)
     задеплоена успешно ещё в 10:38 на обоих приложениях, маркер на бою отвечает
     HTTP 202 — флаг остался висеть, но в полёте у неё ничего не было. Ничего
     чужого этот push не отменяет: сервер и локальная ветка синхронны. -->

🔴 ДРУГИМ СЕССИЯМ: `frontend/Basis/build/` в `.gitignore` — собранный фронт в репозиторий
НЕ коммитим (Timeweb собирает сам; 550 коммитов сборки раздули репо до 639 МБ).

<!-- Освобождено 2026-08-03 10:10. Деплой ЧИНИТСЯ САМ НЕ БЫЛ — он лежал с 02.08 19:04 по
     причине на стороне Timeweb (см. docs/timeweb-support-ticket.md и журнал: контрольный
     эксперимент — коммит, собравшийся успешно часом ранее, падал за 21 с). 03.08 в 10:00
     оба приложения собрались успешно: фронт 659 с, бэк 373 с, коммит 708095a7.
     Проверено: бандл на бою main.08d9186d.js, `verify-live.js` — 20 из 20, карты сайта
     4692 + 3087 + 85, API отвечает. -->

🔴 ДРУГИМ СЕССИЯМ: `frontend/Basis/build/` теперь в `.gitignore` — собранный фронт в репозиторий
НЕ коммитим (Timeweb собирает сам, доказано разницей бандлов; 550 коммитов сборки раздули репо
до 639 МБ). Не возвращайте её и не делайте `git add …build`.

<!-- Освобождено 2026-08-02 (ночь), сессия «геополитика / портрет очага».
     Проверено ПРОТОКОЛОМ, а не наугад — scripts/check_deploy.sh:
       • маркеры гео-сессии (obs-profile-watch, «Что это значит для рынка»,
         obs-geo-dq, «Что стоит за выводом») — ✓ в живом бандле;
       • маркер сессии «лента/Важное» («Экстраординарных событий») — ✓ там же.
     То есть сводный пуш доехал целиком, обе работы на бою. -->

<!-- Освобождено: последний push (22a1cb94da) — только docs/CLAUDE.md, фронтенд
     не тронут, нечего верифицировать по бандлу. Правки шрифтов SEO-статики
     (7b0f986c40) уже подтверждены на бою до этого push. -->

<!--
Когда занимаешь очередь, замени блок выше на:

## Статус: ЗАНЯТО

- Сессия: <коротко тема — напр. "регистрация/тост">
- Начала: <дата время>
- Пушит: <что именно — коммит(ы) кратко>
- Ждёт: деплоя (смены main.<hash>.js на бою)

Когда бандл на бою сменился (подтверждено curl) — верни файл к виду
"## Статус: ЗАНЯТО

- Сессия: SEO / страницы инструментов
- Начала: 2026-08-02 18:05
- Пушит: чекпойнт журнала; в сборку должен войти уже запушенный 25344b3034
  (переключение раздела при открытии бумаги — на бою его ещё нет)
- Ждёт: деплоя (смены main.<hash>.js на бою)" ОДНИМ коммитом (можно вместе со своим следующим push,
если он тоже готов).

Если статус УЖЕ "ЗАНЯТО" другой сессией — НЕ пуши. Либо подожди (проверяй
периодически), либо продолжай работу, которая не требует push прямо сейчас,
и вернись к пушу позже.
-->
