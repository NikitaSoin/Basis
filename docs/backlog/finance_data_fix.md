# Финансовые данные — закрытие дыр + единицы

## Диагностика (2026-07-03, по 264 компаниям)
- **Пустоты:** P&L пуст/>50% null у **101/264**, мультипликаторы у **101/264**, баланс у **74/264**,
  ОДДС отсутствует у 12 (в осн. банки — у них нет классического ОДДС, легитимно).
- **Голубые фишки (47) с дырами — ПРИОРИТЕТ (14):**
  - Полные (P&L+баланс+мультипл): HYDR, OZON
  - P&L: NVTK, ROSN, SVCB
  - баланс: LENT, VTBR
  - мультипликаторы: HEAD, MDMG, MTLR, MVID, RUAL, SNGS, YDEX
- **Единицы:** 250 «млн», 11 «млрд» (проверены — корректны, данные реально в млрд), 3 нестандартные:
  GTRK/NFAZ/URKZ = «тысячи» → унифицировать в млн. Грубых смешений масштаба ВНУТРИ компаний не найдено.

## План
1. **Финансист (Sonnet) пакетно** закрывает дыры. ПИЛОТ — HYDR (в работе). Затем 14 голубых фишек,
   потом хвост по ликвидности. Источник: web-search первички (e-disclosure/сайт/smart-lab) +
   report-fetcher для постатейной глубины. Не выдумывать — null+флаг.
2. **Единицы в данных** — финансист следит за единым масштабом meta.unit; 3 нестандартные унифицирует.
3. **Какие показатели в Финансы** — финансист предлагает по каждой (динамика маржи, ROE/ROA, долг.
   нагрузка по годам), решаем какие выводить.

## 🖥️ ФРОНТ-задача (отдельно, в basis-designer): adaptive unit formatting
Проблема владельца «выручка в трлн, а подписано млрд» — это ОТОБРАЖЕНИЕ, не данные. У компании
unit=млн, выручка напр. 21 000 000 (млн = 21 трлн) рендерится как «21000000» с подписью «млрд».
Решение: на фронте (App.js / компонент Финансы) форматировать число АДАПТИВНО по величине —
млн→млрд→трлн автоматически, и у КАЖДОГО числа показывать корректную единицу (фидбек владельца
«лучше у каждой цифры писать что это»). Не хранить разные единицы в данных — единый meta.unit +
умное форматирование на выводе.

## Детальный банковский баланс (данные — финансист)
Фронт детализировал банк P&L (5→14 строк) и метрики (5→11) — данные уже были. НО банковский
БАЛАНС в данных только тотал (нет структуры). Финансист добывает по банкам (SBER/VTBR/T/SVCB/BSPB):
кредитный портфель (валовой/резервы/чистый; корп/розница), портфель ценных бумаг (ОФЗ/облигации),
средства клиентов (депозиты физики/юрики, текущие/срочные), МБК. Код готов принять — метка
`TODO(bank-balance)` в App.js ~4070; после данных bsRows банков расширится по аналогии с non-bank.

## КАНОН банковского баланса (для единообразия — финансисты и фронт следуют)
В `balance_sheet` (НЕ отдельный bank_balance!), массивы по годам, единый meta.unit:
`gross_loans` (валовой кредитный портфель), `loan_provisions` (резервы), `net_loans` (чистый),
`loans_corporate` / `loans_retail` (split net, опц), `securities` (портфель ц.бумаг),
`customer_deposits` (средства клиентов всего), `deposits_retail` / `deposits_corporate` (split, опц),
`due_to_banks` (МБК). T/SBER/VTBR нормализованы к этому канону. BSPB/SVCB делать сразу по нему.
Фронт (designer bsRows банка, метка TODO(bank-balance)) читает эти поля.
