# Обращение в поддержку Timeweb Cloud — текст для отправки

> Скопировать всё, что ниже разделителя, в тикет. Данные собраны через ваш же API
> (`api.timeweb.cloud/api/v1`), поэтому id и время точные, а не «примерно».

---

Здравствуйте.

С 2 августа не проходит ни один деплой **двух** приложений в проекте 2443177:

- **InBasis**, id `182419`, тип frontend (react), сборка `npm run build`, каталог `/frontend/Basis`
- **inbasis-backend**, id `218073`, тип backend (fastapi), сборка `pip install --upgrade -r requirements.txt`

Оба берут код из одного публичного репозитория `NikitaSoin/Basis`, ветка `main`.

**Последний успешный деплой у обоих — 2026-08-02 19:04:52 UTC**, коммит `48ad32dc09`
(фронт собирался 384 с, бэкенд 290 с). После него подряд: 22 неудачи у фронта и 18 у бэкенда.

**Главное: дело не в нашем коде.** Я развернул тот же самый коммит `48ad32dc09`, который
за час до этого собрался успешно, — он упал за 21 секунду (деплой
`be62185a-bb6c-4986-9526-307ffcf42a5e`, старт 2026-08-02 22:44:59 UTC). Код не менялся,
изменилось поведение платформы.

**Деплои падают быстрее, чем физически может пройти сборка:**

| приложение | id деплоя | старт (UTC) | длительность |
|---|---|---|---|
| InBasis | `be62185a-bb6c-4986-9526-307ffcf42a5e` | 22:44:59 | **21 с** |
| InBasis | `79fb8fab-8ba7-4899-9d67-fdb10a7f09cf` | 22:43:59 | **9 с** |
| InBasis | `dd2bced9-c31d-4fac-9ae4-8b2caaf301ab` | 22:30:37 | **6 с** |
| inbasis-backend | `6691e760-df8d-4766-a7a9-88bfac21991d` | 22:40:11 | **42 с** |
| inbasis-backend | `e3391fdd-2d4f-4cb3-9d77-92165d2d7f33` | 22:19:40 | **2 с** |

Для сравнения: полное клонирование этого репозитория занимает **89 секунд** (замерено),
а успешные сборки шли 290–384 секунды. Деплой, который заканчивается за 6–9 секунд, не
успевает даже получить исходный код.

**Логи сборки пустые** — и в панели, и через API (`/apps/{id}/deploys/{deploy_id}/build-logs`
отвечает 404, `/apps/{id}/logs` отдаёт только журнал уже работающего приложения). То есть
сборка не начинается, а не падает внутри.

**Что проверено с нашей стороны и исключено:**

1. Обе сборки проходят локально на тех же версиях инструментов, что у вас: Node 24 (фронт
   собирается целиком, 8831 файл), Python 3.14 (`pip install --upgrade -r requirements.txt`
   ставит все 51 пакет без ошибок).
2. Команды и каталоги сборки не менялись с момента последнего успеха.
3. Аккаунт не заблокирован, баланс 550 ₽, запас 261 час. Диск бэкенда занят на 7 из 30 ГБ.
4. Приложения при этом **работают** — сайт и API отвечают на старой версии.

Просьба посмотреть с вашей стороны, почему задания сборки завершаются мгновенно и почему
не пишутся логи. Если проблема в квоте, ресурсах билд-окружения или в связке с GitHub —
подскажите, что поправить.
