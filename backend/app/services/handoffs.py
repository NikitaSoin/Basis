"""Передачи между тремя аналитиками — как КОНТРАКТ полей, а не как беседа.

🔴 ЗАЧЕМ (владелец, 2026-09-13, пункт 3 плана). Шесть методичек-связок
описывают, что один аналитик обязан передать другому. До сих пор передача была
одна и неформальная: сводка соседа целиком вкладывалась в задание, и агент сам
решал, что оттуда взять. «Недоученное» было невидимо. Здесь каждая из шести
передач — набор именованных полей: производитель ОБЯЗАН их заполнить (гейт
пишет заметку на каждое пустое), потребитель ОБЯЗАН их прочитать (они лежат в
задании отдельным блоком с пометкой «контракт»). Пустое поле — видно механически.

Поля взяты из самих методичек:
  ГМ  геополитика → макро    — ГМ 9.1–9.2: шоки по каналам с направлением, силой,
                               лагом, обратимостью; сценарии; пороговые события.
  МГ  макро → геополитика    — ИГ 8.8 и МГ Части 2, 6: оценка экономической
                               выносливости курса — фискальная способность,
                               долговая устойчивость, точки исчерпания, цена курса.
  ИМ  институты → макро      — ИМ 8.2–8.3: чувствительная доля кредита, доли
                               льготного/административного/теневого контуров,
                               заякоренность ожиданий, признаки фискального
                               доминирования, доля инфляции, недоступная ставке.
  МИ  макро → институты      — МИ Части 1–4, 11: происхождение доходов (рента/
                               налоги), близость кризисной развилки, инфляция как
                               разрушитель, качество роста, опережающие
                               экономические индикаторы институциональных сдвигов.
  ГИ  геополитика → институты — ГИ 3.1, 7.1: событие, вектор интенсивности,
                               материальные изменения (доходы государства, доступ к
                               капиталу, экспорт, импорт, технологии, логистика,
                               труд, валюта, стоимость финансирования).
  ИГ  институты → геополитика — ИГ 9.7 шаги 1–5, 8.8: кто решает, у кого ресурс,
                               правящая коалиция и её интересы, ограничения
                               руководства, кто выигрывает от эскалации/деэскалации,
                               сценарии расходов и ограничений.

Каждое поле — словами и цифрами (правило языка), с датой и источником где
уместно; статус утверждения Ф/Д/В/Г там, где это оценка.
"""
from __future__ import annotations

from dataclasses import dataclass, field


@dataclass(frozen=True)
class Handoff:
    code: str                 # ГМ, МГ, ИМ, МИ, ГИ, ИГ
    src: str                  # kind производителя: geo | macro | inst_state
    dst: str                  # kind потребителя
    key: str                  # имя блока в payload производителя: handoffs.<key>
    title: str
    fields: dict[str, str]    # имя поля → что в нём должно быть (для роли и для гейта)
    required: tuple[str, ...] = field(default_factory=tuple)


HANDOFFS: dict[str, Handoff] = {
    "ГМ": Handoff("ГМ", "geo", "macro", "to_macro", "Геополитика → макроэкономика", {
        "shocks": "список шоков: событие, канал из четырнадцати (ГМ Часть 2), направление для "
                  "макропеременной, класс масштаба (малый/умеренный/значимый/режимный), лаг, "
                  "обратимость, горизонт",
        "scenario_probabilities": "вероятности сценариев на 6 и 18 месяцев (из сводки)",
        "threshold_events": "наблюдаемые пороговые события смены сценария (ГМ 9.2)",
        "sector_flags": "секторные флаги с направлением",
        "expected_policy_reaction": "что, по геополитической логике, сделают бюджет и ЦБ",
    }, required=("shocks", "scenario_probabilities", "threshold_events")),

    "МГ": Handoff("МГ", "macro", "geo", "to_geo", "Макроэкономика → геополитика", {
        "fiscal_capacity": "сколько государство может тратить без дестабилизации: баланс, "
                           "источники финансирования, резервы — с числами и датами",
        "debt_sustainability": "устойчивость долга в текущем режиме: уровень, стоимость "
                               "обслуживания, доступ к рынку",
        "exhaustion_points": "точки исчерпания: какой показатель, какой порог, когда при "
                             "текущей траектории (МГ Часть 6)",
        "endurance_horizon_months": "оценка выносливости курса в месяцах со статусом В/Г",
        "price_of_course": "цена текущего внешнеполитического курса для экономики и кто её "
                           "платит (адресат)",
        "adversary_economy": "экономика противника как вход в сценарии конфликта (МГ 11 — протокол "
                             "экономического профиля актора): внешнее финансирование и его условия, "
                             "бюджетный разрыв, состояние энергосистемы, людской ресурс, валюта — "
                             "с источником и статусом Ф/Д/В/Г; нет данных — так и написать",
        "leading_indicators_for_geo": "экономические индикаторы, опережающие геополитические "
                                      "решения (МГ Часть 10)",
    }, required=("fiscal_capacity", "exhaustion_points", "endurance_horizon_months")),

    "ИМ": Handoff("ИМ", "inst_state", "macro", "to_macro", "Институты → макроэкономика", {
        "sensitive_credit_share": "чувствительная к ставке доля кредита и её обоснование (ИМ 4.3)",
        "preferential_contour_share": "доли льготного, административного, теневого и бюджетного "
                                      "контуров — оценки с пометкой",
        "expectations_anchoring": "заякоренность ожиданий: уровень против цели, скорость "
                                  "затухания после шока (ИМ 8.2, λ)",
        "fiscal_dominance_signs": "признаки фискального доминирования — есть/нет и какие (ИМ 3.6)",
        "inflation_inaccessible_to_rate": "доля инфляции, недоступная ставке: административная, "
                                          "санкционно-издержковая, защищённые контуры (ИМ 8.2 п.4)",
        "growth_institutional_component": "институциональный вклад в потенциальный темп (ИМ 8.1 п.5)",
    }, required=("sensitive_credit_share", "expectations_anchoring", "inflation_inaccessible_to_rate")),

    "МИ": Handoff("МИ", "macro", "inst_state", "to_inst", "Макроэкономика → институты", {
        "fiscal_origin": "происхождение доходов государства: доля ренты против налогов, "
                         "динамика — что это говорит о типе государства (МИ Часть 1)",
        "crisis_proximity": "близость кризисной развилки: какие индикаторы, насколько (МИ Часть 3)",
        "inflation_as_destroyer": "инфляция как перераспределитель: кто теряет, кто выигрывает, "
                                  "скорость разрушения доверия (МИ Часть 4)",
        "growth_quality": "качество роста: рыночный или бюджетный контур, кому достаётся (МИ Часть 2)",
        "leading_indicators_for_institutions": "экономические индикаторы, опережающие "
                                               "институциональные сдвиги (МИ Часть 11)",
    }, required=("fiscal_origin", "crisis_proximity", "inflation_as_destroyer")),

    "ГИ": Handoff("ГИ", "geo", "inst_state", "to_inst", "Геополитика → институты", {
        "events": "события периода по таксономии с вектором интенсивности (ГИ 7.1)",
        "material_changes": "что изменилось материально: доходы государства, доступ к капиталу, "
                            "экспорт, импорт, технологии, логистика, труд, валюта, стоимость "
                            "финансирования (ГИ 3.1) — с числами",
        "market_or_administrative": "по каждому материальному изменению: государство отвечает "
                                    "рынком или административным доступом (ГИ 3.1)",
        "threat_structure": "как изменилась структура угроз и внешних альтернатив",
    }, required=("events", "material_changes")),

    "ИГ": Handoff("ИГ", "inst_state", "geo", "to_geo", "Институты → геополитика", {
        "decision_makers": "кто принимает внешнеполитические решения (ИГ 9.7 ш.1)",
        "resource_holders": "кто реально обладает ресурсом (ш.2)",
        "ruling_coalition": "правящая коалиция и интересы участников (ш.3–4)",
        "leadership_constraints": "фактические ограничения руководства (ш.5)",
        "escalation_beneficiaries": "кто внутри выигрывает от эскалации и от деэскалации",
        "spending_and_constraint_scenarios": "сценарии расходов и ограничений для оценки "
                                             "выносливости (ИГ 8.8)",
        "execution_capacity": "способность государства исполнить принятое решение",
    }, required=("ruling_coalition", "leadership_constraints", "escalation_beneficiaries")),
}


def outgoing(src_kind: str) -> list[Handoff]:
    return [h for h in HANDOFFS.values() if h.src == src_kind]


def incoming(dst_kind: str) -> list[Handoff]:
    return [h for h in HANDOFFS.values() if h.dst == dst_kind]


def prompt_block(src_kind: str) -> str:
    """Что производитель ОБЯЗАН заполнить — для роли агента."""
    out = ["\n===== ПЕРЕДАЧИ СОСЕДЯМ (контракт, обязательно) =====",
           "В поле \"handoffs\" заполни блоки для соседних аналитиков. Каждое поле — "
           "словами и цифрами, с датой и источником; где оценка — статус Ф/Д/В/Г. "
           "Пустое обязательное поле — заметка проверки и вопрос от соседа завтра."]
    for h in outgoing(src_kind):
        out.append(f"\n• handoffs.{h.key} — {h.title} ({h.code}):")
        for name, desc in h.fields.items():
            mark = " [обязательно]" if name in h.required else ""
            out.append(f"    \"{name}\"{mark}: {desc}")
    return "\n".join(out) + "\n"


def gate_notes(payload: dict, src_kind: str) -> list[str]:
    """Заметки гейта: какие обязательные поля передач пусты."""
    notes: list[str] = []
    hs = payload.get("handoffs") or {}
    for h in outgoing(src_kind):
        block = hs.get(h.key) if isinstance(hs, dict) else None
        if not isinstance(block, dict):
            notes.append(f"handoffs.{h.key} ({h.code}): блок передачи отсутствует целиком")
            continue
        for name in h.required:
            v = block.get(name)
            if v in (None, "", [], {}):
                notes.append(f"handoffs.{h.key}.{name} ({h.code}): обязательное поле пусто")
    return notes


def incoming_block(dst_kind: str, sources: dict[str, dict | None]) -> str:
    """Что потребитель ОБЯЗАН прочитать — для задания агента.

    sources: {src_kind: payload производителя (или None)}. Берём только блок
    контракта, а не сводку целиком: агент получает ровно то, что ему адресовано."""
    out = ["===== ПЕРЕДАЧИ ОТ СОСЕДЕЙ (контракт — прочитать обязательно) ====="]
    import json
    for h in incoming(dst_kind):
        src = sources.get(h.src)
        block = ((src or {}).get("handoffs") or {}).get(h.key) if isinstance(src, dict) else None
        as_of = (src or {}).get("as_of") if isinstance(src, dict) else None
        if not isinstance(block, dict):
            out.append(f"\n• {h.title} ({h.code}): передача НЕ ЗАПОЛНЕНА соседом"
                       + (f" (его сводка от {as_of})" if as_of else " (сводки нет)")
                       + " — учти это как пробел и, если критично, задай ему вопрос в questions_to_peers")
            continue
        empty = [n for n in h.required if block.get(n) in (None, "", [], {})]
        out.append(f"\n• {h.title} ({h.code}), сводка соседа от {as_of}"
                   + (f"; пустые обязательные поля: {empty}" if empty else "") + ":")
        out.append(json.dumps(block, ensure_ascii=False, default=str)[:12_000])
    return "\n".join(out) + "\n"


# ─────────────── вечерняя сборка: цепочки через соседей и отчёт финала ───────────────
# 🔴 Владелец (2026-09-13): аналитик обязан считывать эффекты длиннее одного ребра
# (институты → геополитика → экономика) и обратные петли (геополитика → экономика →
# обратно в геополитику). Для этого в финале ему даны ПОЛНЫЕ черновики соседей и
# все методички, включая чужие.
CHAINS_RULE = """
===== ЦЕПОЧКИ ЧЕРЕЗ СОСЕДЕЙ (обязательно) =====
Считывай эффекты длиннее одного ребра и обратные петли — для этого тебе даны ПОЛНЫЕ
черновики обоих соседей и все методички, включая чужие. Примеры: событие в институтах →
изменило геополитику → дошло до экономики; геополитика → экономика → обратно в
геополитику (выносливость курса). Открой методички-связки ОБОИХ рёбер цепочки (не
только своего) и разбери по звеньям. Верни поле "cross_chains": [ {"chain": <контур →
контур → контур>, "trigger": <событие с датой>, "links": [ {"edge": <ГМ|МГ|ГИ|ИГ|ИМ|МИ>,
"mechanism", "methodology": <doc:раздел>} ], "effect_on_me": <какой блок/поле и как>,
"status": <Ф|Д|В|Г>} ]. Нет цепочек — так и напиши, почему.
"""

FINAL_FIELDS = (
    '"answers_to_peers": [...] — ответы на ВСЕ вопросы соседей к черновику, с числом и источником; '
    '"contradictions_resolved": [ {"topic", "action": <снято|объяснено>, "detail"} ] — по каждому '
    'противоречию сверки; "critique_resolved": [ {"rule", "where", "action": <исправлено|отклонено>, '
    '"detail"} ] — по каждому замечанию проверяющего; "lessons_applied": [...]; '
    '"cross_chains": [...] — см. блок ЦЕПОЧКИ'
)

ALL_SHELF = ["code", "macro_base", "inst_env", "geo_base", "geo_events", "geo_macro",
             "macro_geo", "geo_inst", "inst_geo", "inst_macro", "macro_inst", "macro_sector",
             "geo_screen",   # спецификация экрана «Оценка ситуации» (владелец, 2026-09-16)
             "inst_screen"]  # спецификация экрана условий для бизнеса (владелец, 2026-09-18)


# ─────────────── мандат старшего аналитика (владелец, 2026-09-13) ───────────────
# 🔴 ЗАЧЕМ. Владелец: «я пытаюсь воссоздать реального старшего аналитика, буквально
# уровня первого лица — агент умеет серьёзно анализировать все системные взаимосвязи и
# понимать, куда всё движется; и дальше эту систему применять к инвестиционным задачам».
# Методички — интеллектуальная прокачка (КАК анализировать); ЗАДАЧА — что именно агент
# обязан понимать про текущую ситуацию — живёт здесь, в системном задании, а не в
# методичках (они физически не могут расписать всё). Ответ ложится в поле "situation":
# у геополитика — в каждом очаге, у экономиста и институционалиста — верхнего уровня.
# Пустое поле — заметка гейта (видно механически, как и передачи).
SENIOR_MANDATE = """
===== МАНДАТ СТАРШЕГО АНАЛИТИКА (владелец, 2026-09-13) =====
Ты не пересказчик ленты и не заполнитель формы. Ты — старший аналитик уровня советника
первого лица: твоя работа — держать в голове ВСЮ текущую ситуацию как систему, понимать,
куда она движется, и переводить это в последствия для российской экономики и рынка.
Форма сводки (разделы, поля, передачи) — способ записи; содержание — твоё системное
понимание. В каждой сборке ты обязан ответить себе и отразить в поле "situation":
1. ЧТО ПРОИСХОДИТ НА САМОМ ДЕЛЕ. Не события, а процессы за ними: какие силы действуют,
   чьи ресурсы растут и чьи истощаются, что ограничивает каждую сторону, что уже исчерпано.
2. КТО УЧАСТНИКИ И ЧЕГО ОНИ ХОТЯТ. По каждой стороне и каждому значимому внешнему игроку:
   интересы; что для него выигрыш и что неприемлемая цена (функция полезности); внутренние
   ограничения — коалиция, экономика, электоральный календарь, общественное мнение; как вёл
   себя в похожих ситуациях; как, вероятно, ответит на следующий шаг противника. Реакции — не
   «возможно всё», а ранжированный список с обоснованием и вероятностью.
3. КУДА ДВИЖЕТСЯ. Направление и темп процесса; наиболее вероятная траектория ОТДЕЛЬНО от
   наиболее опасной; точки перелома и что должно случиться, чтобы направление изменилось;
   что ты будешь наблюдать, чтобы понять, что ошибся.
4. ЧТО ЭТО ЗНАЧИТ ДЛЯ ДЕНЕГ. Цепочка от события через соседние контуры до экономики РФ и
   рынка — конкретными механизмами, а не «риски растут».
5. ЧЕГО ТЫ НЕ ЗНАЕШЬ. Пробелы, которые могли бы перевернуть вывод, и что ты сделал, чтобы их
   закрыть (search_feed, conflict_data, веб, прошлые версии сводок).
У каждого суждения статус Ф/Д/В/Г; у прогнозов — вероятность и уверенность раздельно.
🔴 ВЕРОЯТНОСТЬ: в JSON — число, в тексте — слово, по ОДНОЙ таблице (иначе сводки несравнимы
между прогонами): «крайне маловероятно» ≤0,05; «маловероятно» 0,05–0,20; «возможно» 0,20–0,45;
«скорее да» 0,45–0,70; «вероятно» 0,70–0,90; «почти наверняка» >0,90. Число — середина диапазона,
если нет оснований для точнее. Класс масштаба (малый/умеренный/значимый/режимный) — не число.
Юридические рамки платформы (нейтральный тон, РФ-топонимика, без «купить/продать») действуют
и здесь. Поле situation — аналитическое ядро сводки, не витринный текст: пиши плотно, с
датами, числами и источниками, без общих слов.
"""

MANDATE_GEO = """
ОБЯЗАТЕЛЬНЫЕ ВОПРОСЫ ГЕОПОЛИТИКА (ответ — в regions.<очаг>.situation, для КАЖДОГО очага):
По СВО:
 (а) УДАРЫ ПО ИНФРАСТРУКТУРЕ с обеих сторон: что поражено за период — по собранным данным
     (недельные ряды ударов по классам объектов в задании и инструмент conflict_data): какие
     классы объектов, темп (растёт/падает), куда смещается фокус целей; какой ущерб и его
     обратимость (классом масштаба); продолжится ли кампания, по каким классам объектов и
     почему (с вероятностью); какие ответные действия ожидать от каждой стороны и как это
     дойдёт до экономики (передай экономисту через handoffs.to_macro);
 (б) ХОД БОЕВЫХ ДЕЙСТВИЙ: направление и темп изменения контроля (площадь по ISW и заявления
     о населённых пунктах в conflict_data), у кого инициатива и за счёт чего, насколько
     устойчива динамика; без оперативно-тактических подробностей — только то, что меняет
     вероятности сценариев;
 (в) УСТОЙЧИВОСТЬ СТОРОН: ресурсная база каждой. Для Украины — внешнее финансирование и его
     условия, бюджет, энергетика, людской ресурс, состояние экономики (доищи: search_feed по
     украинским и западным лентам, веб). Для России — по передачам экономиста (выносливость
     курса) и институционалиста (кто решает, ограничения руководства);
 (г) ВНЕШНИЕ ИГРОКИ: США, ЕС и ключевые столицы, Китай, Турция, Глобальный Юг — интересы и
     ограничения; ЭЛЕКТОРАЛЬНЫЕ ЦИКЛЫ и внутриполитические календари (выборы, бюджетные
     процессы, смены правительств) и как они сдвигают политику по конфликту, санкциям и
     помощи; что каждый сделает при эскалации и при движении к сделке;
 (д) ПЕРЕГОВОРНЫЕ ТРЕКИ: кто с кем, о чём реально, красные линии сторон, что мешает.
По Ближнему Востоку и АТР — те же вопросы в масштабе очага: стороны и их интересы, ход
противостояния, устойчивость, внешние игроки, куда движется, плюс канал до российского рынка
(нефть, логистика, платежи, технологии).
Форма поля situation у КАЖДОГО очага:
 "situation": {"what_is_happening": "<процессы за событиями, 3-6 предложений с датами и числами>",
   "actors": [ {"actor", "interests", "utility": "<выигрыш / неприемлемая цена>", "constraints",
                "expected_next": "<вероятный следующий шаг>", "status": "Ф|Д|В|Г"} ],
   "strikes": {"period": "<что поражено, классы объектов, темп по данным>",
               "damage": "<ущерб и обратимость, класс масштаба>",
               "outlook": "<продолжится ли, по каким классам объектов, вероятность и почему>",
               "expected_responses": [ {"side", "response", "probability", "why"} ]},
   "battlefield": {"direction", "tempo", "initiative": "<у кого и за счёт чего>", "sustainability", "status"},
   "sustainability": {"<сторона>": "<ресурсная база, внешняя поддержка, точки исчерпания>"},
   "external_players": [ {"actor", "interest", "electoral_or_domestic_calendar", "effect_on_policy"} ],
   "trajectory": {"most_likely", "most_dangerous", "turning_points": [..], "watch": [..]},
   "unknowns": [ {"gap", "why_it_matters", "what_i_did"} ] }
Для middle_east и atr поля strikes/battlefield заполняй, если есть удары и военная фаза; иначе null.
"""

MANDATE_MACRO = """
ОБЯЗАТЕЛЬНЫЕ ВОПРОСЫ ЭКОНОМИСТА (ответ — в поле "situation" верхнего уровня):
 (а) ЭКОНОМИКА КАК СИСТЕМА: не набор показателей, а механизм — какие адаптации к шокам уже
     произошли и исчерпаны, где узкие места (мощности, труд, импорт, расчёты), что держит
     систему и что её ломает;
 (б) ВОЕННЫЕ СОБЫТИЯ → ЭКОНОМИКА: удары по инфраструктуре (по передаче геополитика → макро и
     по данным conflict_data): какие мощности и логистика задеты, что это значит для выпуска,
     цен на топливо, логистики, инфляции, бюджета; что будет, если кампания продолжится по тем
     же классам объектов (сценарно); что уже видно в данных, а что — ожидание;
 (в) РЕАКЦИЯ ВЛАСТЕЙ: ЦБ, Минфин, правительство — их функции полезности (что защищают в первую
     очередь: инфляцию, бюджет, курс, занятость, финансирование обороны), ограничения,
     наиболее вероятные следующие решения и их последствия;
 (г) УКРАИНСКАЯ ЭКОНОМИКА И ВНЕШНИЙ ФОН: устойчивость противника как вход в сценарии (внешнее
     финансирование, бюджет, энергетика) — коротко, для геополитика (через handoffs.to_geo);
     мировые ставки, нефть, Китай — как они меняют ограничения для России;
 (д) КУДА ДВИЖЕТСЯ: траектория на 6 и 18 месяцев, наиболее вероятная ОТДЕЛЬНО от наиболее
     опасной, точки перелома, что наблюдать.
Форма: "situation": {"what_is_happening", "adaptation_and_bottlenecks",
   "war_to_economy": {"infrastructure_damage", "channels", "if_strikes_continue", "seen_in_data_vs_expected"},
   "authorities": [ {"actor", "utility", "constraints", "expected_next", "consequence"} ],
   "ukraine_and_external": "<...>",
   "trajectory": {"most_likely", "most_dangerous", "turning_points": [..], "watch": [..]},
   "unknowns": [ {"gap", "why_it_matters", "what_i_did"} ] }
"""

MANDATE_INST = """
ОБЯЗАТЕЛЬНЫЕ ВОПРОСЫ ИНСТИТУЦИОНАЛИСТА (ответ — в поле "situation" верхнего уровня):
 (а) СИСТЕМА ВЛАСТИ КАК ЦЕЛОЕ: кто решает, чьи интересы представлены, где реальные, а не
     формальные центры решений; функция полезности правящей коалиции (что защищает любой
     ценой, чем готова жертвовать) и её ограничения;
 (б) ВОЙНА И ЭКОНОМИКА → ИНСТИТУТЫ: как удары по инфраструктуре, ход боевых действий, санкции и
     фискальное давление (по передачам соседей) двигают дрейф — какие механизмы включены:
     семь механизмов конвертации давления — по методичке geo_inst Часть 4 (мобилизационный,
     замещения, концентрации и огосударствления, закрытия информации, лояльности, фискального
     голода, правового дуализма), типы динамики — inst_env Часть 5–6; называй механизм ИМЕНЕМ
     из методички, где это уже видно в решениях и практике, что ожидать дальше;
 (в) РЕАКЦИЯ СИСТЕМЫ: как государство отвечает на удары и шоки внутри страны (полномочия,
     изъятия, регулирование, информационные меры) — ранжированные ожидаемые ходы с обоснованием;
 (г) ВНЕШНИЕ ИГРОКИ И ЭЛИТЫ: как электоральные и внутриполитические циклы западных стран меняют
     санкционную и переговорную политику и через это — стимулы внутри страны (в связке с
     геополитиком); внутриэлитные балансы — кто выигрывает от эскалации/деэскалации;
 (д) КУДА ДВИЖЕТСЯ: траектория институциональной среды на 6 месяцев и 2 года, наиболее
     вероятная ОТДЕЛЬНО от наиболее опасной, точки перелома, что наблюдать.
Форма: "situation": {"what_is_happening",
   "power_system": {"who_decides", "coalition_utility", "constraints"},
   "war_to_institutions": {"pressures", "mechanisms_active", "seen_in_practice", "expected_next"},
   "system_responses": [ {"response", "probability", "why"} ],
   "external_and_elites",
   "trajectory": {"most_likely", "most_dangerous", "turning_points": [..], "watch": [..]},
   "unknowns": [ {"gap", "why_it_matters", "what_i_did"} ] }
"""

MANDATES = {"geo": MANDATE_GEO, "macro": MANDATE_MACRO, "inst_state": MANDATE_INST}

# ─────────────── геополитический снимок по новым методичкам (Г 12.1 + ИГ Часть 2) ───────────────
# 🔴 Владелец (2026-09-14): старый формат сводки (13 показателей, S1–S4) — из прежней геосистемы;
# новые методички требуют снимок по Г 12.1 (четырнадцать блоков) и карты решений по ИГ 2.2–2.6.
# Поле geo_snapshot добавляется РЯДОМ со старыми полями: потребители старого формата (витрина,
# карточки, сценарные сдвиги) не трогаются, пока не переедут. Пропавший блок переносится со вчера.
GEO_SNAPSHOT_BLOCKS: list[tuple[str, str]] = [
    ("system", "1. Международная система: полярность, центры силы, региональные подсистемы"),
    ("country_position", "2. Положение страны: ресурсы, зависимости, союзы, уязвимости"),
    ("regime", "3. Внутренний режим: кто принимает решения и какова цена ошибки"),
    ("actors", "4. Акторы: кто реально влияет"),
    ("goals", "5. Цели: что хочет каждая сторона"),
    ("mental_models", "6. Ментальные модели: как стороны понимают ситуацию"),
    ("conflict", "7. Текущий конфликт: что происходит объективно"),
    ("military_balance", "8. Военный баланс: кто чем располагает и как меняется соотношение"),
    ("time_balance", "9. Временной баланс: на чьей стороне время"),
    ("negotiation_balance", "10. Переговорный баланс: какие условия соглашения достижимы"),
    ("scenarios", "11. Сценарии: вероятные траектории с триггерами"),
    ("risks", "12. Риски: что может неожиданно изменить траекторию"),
    ("macro_effect", "13. Макроэкономический эффект: торговля, сырьё, капитал, курс, инфляция, бюджет, инвестиции, потенциал"),
    ("institutional_effect", "14. Институциональный эффект: силовой блок, исполнительная власть, гражданские институты, госприсутствие, собственность, конкуренция, коалиция"),
]
GEO_SNAPSHOT_MARKS = ("факт", "оценка", "гипотеза", "сценарий")
DECISION_MAPS: list[tuple[str, str]] = [
    ("A_decisions", "A. Архитектура принятия внешнеполитических решений (ИГ 2.2)"),
    ("B_resources", "B. Ресурсы (ИГ 2.3)"),
    ("C_interests", "C. Интересы (ИГ 2.4)"),
    ("D_dependencies", "D. Внешние зависимости (ИГ 2.5)"),
    ("E_links", "E. Международные связи (ИГ 2.6)"),
]

GEO_SNAPSHOT_SPEC = (
    "\n===== ГЕОПОЛИТИЧЕСКИЙ СНИМОК ПО МЕТОДИЧКЕ (Г 12.1 + ИГ Часть 2) — поле geo_snapshot, обязательно =====\n"
    "Помимо прежних полей верни \"geo_snapshot\": {\"as_of\": <дата>, \"blocks\": [ {\"key\": <ровно один из: "
    + ", ".join(k for k, _ in GEO_SNAPSHOT_BLOCKS) + ">, \"title\": <соответственно>, "
    "\"text\": <плотно, с датами и числами, 3–8 предложений>, \"mark\": <" + "|".join(GEO_SNAPSHOT_MARKS) + ">, "
    "\"delta_vs_prev\": <что изменилось со вчера или «без изменений»>} ] — все четырнадцать блоков, "
    "\"decision_maps\": { <актор: россия|украина|сша|ес|китай|турция|…>: { "
    + ", ".join(f"\"{k}\"" for k, _ in DECISION_MAPS) + " } } — карты по ИГ 2.2–2.6 для сторон конфликта и "
    "ключевых внешних игроков (кто решает, ресурсы, интересы, зависимости, связи; статус Ф/Д/В/Г у утверждений) }.\n"
    "Названия блоков: " + "; ".join(t for _, t in GEO_SNAPSHOT_BLOCKS) + ".\n"
    "🔴 Это снимок ПО НОВЫМ методичкам, он не заменяет пока прежние поля витрины (subindices, scenario, regions) — "
    "заполняй и то и другое; снимок — аналитическое ядро, витрина — потребитель.\n"
)


def geo_snapshot_gate(fresh: dict, prev: dict | None) -> list[str]:
    """Каркас снимка держит код: четырнадцать блоков, маркировка из списка, пропавший блок — со
    вчера (как у состояния экономики), отсутствие снимка целиком — заметка, не отклонение."""
    notes: list[str] = []
    snap = fresh.get("geo_snapshot")
    prev_snap = (prev or {}).get("geo_snapshot") if isinstance(prev, dict) else None
    if not isinstance(snap, dict) or not isinstance(snap.get("blocks"), list):
        if isinstance(prev_snap, dict):
            fresh["geo_snapshot"] = dict(prev_snap); fresh["geo_snapshot"]["carried_over"] = True
            notes.append("geo_snapshot: не вернулся — перенесён со вчера целиком")
        else:
            notes.append("geo_snapshot: отсутствует (Г 12.1) — снимок по новым методичкам не собран")
        return notes
    have = {b.get("key"): b for b in snap["blocks"] if isinstance(b, dict)}
    prev_have = {b.get("key"): b for b in ((prev_snap or {}).get("blocks") or []) if isinstance(b, dict)}
    rebuilt = []
    for key, title in GEO_SNAPSHOT_BLOCKS:
        b = have.get(key)
        if b is None and key in prev_have:
            b = dict(prev_have[key]); b["carried_over"] = True
            notes.append(f"geo_snapshot.{key}: блок не вернулся — перенесён со вчера")
        if b is None:
            b = {"key": key, "title": title, "text": None, "mark": "гипотеза", "data_flag": "нет данных в этом прогоне"}
            notes.append(f"geo_snapshot.{key}: блока нет ни сейчас, ни раньше — пустая заготовка")
        b.setdefault("key", key); b.setdefault("title", title)
        if b.get("mark") not in GEO_SNAPSHOT_MARKS:
            b["mark"] = "оценка"; notes.append(f"geo_snapshot.{key}: маркировка не из списка → «оценка»")
        rebuilt.append(b)
    snap["blocks"] = rebuilt
    if not isinstance(snap.get("decision_maps"), dict) or not snap["decision_maps"]:
        if isinstance(prev_snap, dict) and prev_snap.get("decision_maps"):
            snap["decision_maps"] = prev_snap["decision_maps"]
            notes.append("geo_snapshot.decision_maps: не вернулись — перенесены со вчера")
        else:
            notes.append("geo_snapshot.decision_maps: пусто (ИГ 2.2–2.6)")
    return notes


def mandate_block(contour: str) -> str:
    """Общий мандат + обязательные вопросы контура — в системное задание аналитика."""
    return SENIOR_MANDATE + MANDATES.get(contour, "")


_SITUATION_CORE = ("what_is_happening", "trajectory", "unknowns")


def situation_gate_notes(payload: dict, contour: str) -> list[str]:
    """Пустое или обрубленное поле situation — заметка гейта (не отклонение: сводка
    без системного разбора хуже, чем с ним, но лучше, чем никакой). У геополитика
    проверяется каждый очаг."""
    notes: list[str] = []

    def _check(sit, label: str):
        if not isinstance(sit, dict) or not sit:
            notes.append(f"{label}: situation пусто — мандат старшего аналитика не выполнен")
            return
        empty = [k for k in _SITUATION_CORE if not sit.get(k)]
        if empty:
            notes.append(f"{label}: situation без {empty}")
        if contour == "geo" and label.endswith("svo") and not (sit.get("strikes") and sit.get("battlefield")):
            notes.append(f"{label}: situation по СВО без strikes/battlefield — вопросы (а)-(б) мандата не разобраны")

    if contour == "geo":
        regions = payload.get("regions") or {}
        if not isinstance(regions, dict) or not regions:
            return ["regions: пусто — situation по очагам не проверить"]
        for rkey, reg in regions.items():
            if isinstance(reg, dict):
                _check(reg.get("situation"), f"{rkey}")
    else:
        _check(payload.get("situation"), "situation")
    return notes


def final_gate_notes(fresh: dict, questions: list, contradictions: list, critique: list) -> list[str]:
    """Финал вечерней сборки обязан отчитаться по каждому входу: вопросы соседей,
    противоречия сверки, замечания проверяющего, цепочки. Пустой отчёт при непустом
    входе — заметка: видно механически, что не доработано."""
    notes = []
    if questions and len(fresh.get("answers_to_peers") or []) < len(questions):
        notes.append(f"answers_to_peers: ответов {len(fresh.get('answers_to_peers') or [])} на {len(questions)} вопросов соседей")
    if contradictions and len(fresh.get("contradictions_resolved") or []) < len(contradictions):
        notes.append(f"contradictions_resolved: разобрано {len(fresh.get('contradictions_resolved') or [])} из {len(contradictions)}")
    if critique and len(fresh.get("critique_resolved") or []) < len(critique):
        notes.append(f"critique_resolved: отчёт по {len(fresh.get('critique_resolved') or [])} из {len(critique)} замечаний")
    if not fresh.get("cross_chains"):
        notes.append("cross_chains: цепочки через соседей не разобраны (или не объяснено, почему их нет)")
    return notes
