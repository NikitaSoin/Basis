# Очередь push/деплоя — семафор, НЕ журнал

Этот файл живёт в состоянии «сейчас», не копится историей. Перед `git push`
прочитай его целиком. Правило и причина — в `CLAUDE.md`, раздел «Параллельные
сессии» (⚙️ Технические правила).

## Статус: ЗАНЯТО

- Сессия: карточки инструментов — перевод на канон Basis
- Начала: 2026-08-07
- Пушит: фьючерс/облигация/фонд/валюта-металлы на общий словарь styles/instrument-card.css
  (Fraunces, IBM Plex Mono, медный акцент, три уровня прозы, эпистем-теги); попутно убран
  дубль KPI-плиток облигации
- Ждёт: маркера «ic-verdict» в бандле + вида карточек на бою

<!-- Отметку предыдущей сессии («график/прогноз», висела с 2026-08-06) снял, ПРОВЕРИВ, что
     её ожидание выполнено: check_deploy.sh — маркер «cpro--full» ✓ в живом бандле
     main.fb242f82.js, страница /company/ROSN/grafik/ отдаёт 200. Деплой доехал, семафор
     остался висеть. -->

