# Очередь push/деплоя — семафор, НЕ журнал

Этот файл живёт в состоянии «сейчас», не копится историей. Перед `git push`
прочитай его целиком. Правило и причина — в `CLAUDE.md`, раздел «Параллельные
сессии» (⚙️ Технические правила).

## Статус: ЗАНЯТО

- Сессия: лента/«Важное» — СВОДНЫЙ ПУШ за всю очередь (решение владельца 2026-08-02)
- Пушит: всё накопленное разом — фильтр «Важное» (2f7f0ec094), 4 коммита гео-сессии
  (уже на GitHub), + batch незакоммиченных данных (карточки/снапшоты/агенты, git add -u)
- Контекст: Timeweb НЕ стартует билды с 18:53 при живых пушах — после сводного пуша,
  если билд опять не начнётся, нужен ручной деплой из панели (вебхук, у сессий доступа нет)
- ⚠ Поправка к снятой записи: деплой «ленты» НЕ доехал — маркер «Значимое» в
  main.42236d02.js это СТАРЫЙ код (прошлый деплой этой же ленты); новый маркер
  «Экстраординарных событий» в бандле отсутствует. Классический ложный греп по словам.

<!-- Предыдущая запись (сессия «лента / фильтр Важное») снята: её деплой
     ДОЕХАЛ — маркер «Значимое» присутствует в живом бандле
     main.42236d02.js (проверено curl по содержимому, а не по хэшу).
     Семафор остался занятым, видимо, потому что сессия завершилась раньше,
     чем дождалась подтверждения. -->

<!--
Когда занимаешь очередь, замени блок выше на:

## Статус: ЗАНЯТО

- Сессия: <коротко тема — напр. "регистрация/тост">
- Начала: <дата время>
- Пушит: <что именно — коммит(ы) кратко>
- Ждёт: деплоя (смены main.<hash>.js на бою)

Когда бандл на бою сменился (подтверждено curl) — верни файл к виду
"## Статус: ЗАНЯТО

- Сессия: SEO / страницы инструментов
- Начала: 2026-08-02 18:05
- Пушит: чекпойнт журнала; в сборку должен войти уже запушенный 25344b3034
  (переключение раздела при открытии бумаги — на бою его ещё нет)
- Ждёт: деплоя (смены main.<hash>.js на бою)" ОДНИМ коммитом (можно вместе со своим следующим push,
если он тоже готов).

Если статус УЖЕ "ЗАНЯТО" другой сессией — НЕ пуши. Либо подожди (проверяй
периодически), либо продолжай работу, которая не требует push прямо сейчас,
и вернись к пушу позже.
-->
